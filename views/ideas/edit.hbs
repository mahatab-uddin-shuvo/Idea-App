<form action="/ideas/{{idea._id}}?_method=PUT" method="POST" enctype="multipart/form-data">
    <div class="form-group">
        <label for="title">Title</label>
        <input type="text" name="title" class="form-control" id="title" value="{{idea.title}}">
    </div>
    <div class="form-group">
        <label for="description">Description</label>
        <textarea name="description" class="form-control"
        id="description" rows="5" >{{idea.description}}</textarea>
    </div>
    <div class="form-group">
        <label for="status">Status</label>
        <select name="status" id="status">

            <option value="public" {{compareValues "public" idea.status 'select'}}>
                Public
            </option>
            <option value="private" {{compareValues "private" idea.status 'select'}}>
                Private
            </option>
        </select>
    </div>

    <div class="form-group">
        <label for="categories">Categories</label>
        <select name="categories" multiple class="form-control" id="categories">
            {{#each ideaCategories}}
            <option value="{{category}}" {{compareValues category categoryName 'select'}}>
                {{category}}
            </option>
            {{/each}}
        </select>
      </div>

    <div class="form-group">
        <label for="tags">Tags</label>
        <input type="text" name="tags" class="form-control" 
        id="tags" data-role="tagsinput" value="{{idea.tags}}">
    </div>
    <div class="form-check">
        <input type="checkbox" name="allowComments" class="form-check-input"
          id="allowComments" 
          {{#if idea.allowComments}} 
          checked="true" 
          {{/if}}>
        <label for="allowComments" class="form-check-label">AllowComments</label>
    </div>

    <br>
    <div class="form-group">
         <label for="ideaImage">Upload Feature Image to update existing one</label>
         <input type="file" name="ideaImage" id="ideaImage" class="form-control">
    </div>

    <button disabled style="display: none;" class="btn btn-primary mt-3" type="submit">update idea</button>
    <button class="btn btn-primary mt-3" type="submit">Update Idea</button>

</form>